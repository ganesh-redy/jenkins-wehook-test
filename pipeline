pipeline {
    agent any

    stages {
        stage('Webhook Trigger Test') {
            steps {
                echo "✅ Webhook triggered successfully!"
                sh '''
                    echo "Hello from Jenkins pipeline!"
                    date
                '''
            }
        }
	
    stage('Webhook Trigger Test') {
            steps {
                echo "✅ Webhook triggered successfully!"
                sh '''
                    echo "Hello from Jenkins pipeline!"
                    date
                '''
            }
        }


    
    }
}

